services:
  db:
    container_name: "db"
    image: postgres:14.6-alpine
    restart: always
    environment:
      - POSTGRES_USER=fapi
      - POSTGRES_PASSWORD=fapi
      - POSTGRES_DB=fapi
    ports:
      - "15432:5432"
    networks:
      - custom
networks:
  custom:
    driver: bridge